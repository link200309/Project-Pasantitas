export interface PublishedInternship{
    title: string,
    description?: string;
    location: string;
    workMode: string;
    postedAt: string;
    salary: string;
    duration: string;
    area: string;
    applicants: number;
}